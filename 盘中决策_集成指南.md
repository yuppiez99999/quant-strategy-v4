# 盘中实时决策模块 - 集成指南

## 📋 模块概述

盘中实时决策模块(`utils/intraday_decision.py`)将GLM5 AI自动决策引擎集成到量化交易系统中,实现:

- ✅ **实时行情获取**: 使用新浪API批量获取实时价格
- ✅ **AI驱动决策**: 调用GLM5分析市场数据和持仓数据
- ✅ **风险预警监控**: 自动检测止损/止盈/仓位超标
- ✅ **定时决策生成**: 支持定时检查和持续监控
- ✅ **报告自动生成**: Markdown格式决策报告

---

## 🚀 快速开始

### 方式1: 快速测试(推荐)

```bash
cd e:\各种PY程序\11_量化策略
python intraday_decision_test.py
```

**预期输出:**
```
[1/4] 初始化监控器... ✓
[2/4] 加载持仓数据... ✓ (38只持仓)
[3/4] 生成交易决策... ✓ (10-30秒)
[4/4] 导出决策报告... ✓

报告位置: reports/2026-06-23/盘中决策_*.md
```

### 方式2: 只执行一次

```bash
python intraday_decision_test.py --once
```

### 方式3: 持续监控

```bash
# 每5分钟检查一次
python intraday_decision_test.py --interval 300

# 每10分钟检查一次
python intraday_decision_test.py --interval 600
```

---

## 💻 代码集成

### 集成到日报生成流程

```python
# 文件: daily_report.py
from utils.intraday_decision import IntradayDecisionMonitor

def generate_daily_report():
    """生成包含AI决策的日报"""
    
    # 1. 创建监控器
    monitor = IntradayDecisionMonitor(
        api_model='glm-4-plus',
        check_interval=300,
        enable_notifications=False,
    )
    
    # 2. 加载持仓
    monitor.load_positions()
    
    # 3. 生成决策
    decision = monitor.generate_decision()
    
    # 4. 将AI分析插入日报
    report = f"""# 每日交易日报 - {datetime.now().strftime('%Y-%m-%d')}

## AI 智能决策

{decision.market_summary}

## 风险预警
"""
    for alert in decision.risk_alerts:
        report += f"- [{alert.severity}] {alert.message}\n"
    
    # 5. 保存报告
    save_report(report)
    
    return decision
```

### 集成到Streamlit UI

```python
# 文件: ui/pages/05_🤖_盘中决策.py
import streamlit as st
from utils.intraday_decision import IntradayDecisionMonitor

st.set_page_config(page_title="盘中实时决策", layout="wide")
st.title("🤖 GLM-5 盘中实时决策")

# 侧边栏配置
with st.sidebar:
    st.header("配置")
    interval = st.slider("检查间隔(秒)", 60, 3600, 300)
    api_model = st.selectbox("模型", ["glm-4-plus", "glm-4-flash"])
    
    if st.button("开始监控", type="primary"):
        monitor = IntradayDecisionMonitor(
            api_model=api_model,
            check_interval=interval,
        )
        
        # 显示实时状态
        status = st.empty()
        status.info("监控已启动...")
        
        # 执行一次决策
        decision = monitor.generate_decision()
        
        if decision:
            st.success("决策生成完成!")
            
            # 交易信号
            st.subheader("交易信号")
            if decision.trading_signals:
                for sig in decision.trading_signals:
                    st.write(f"**[{sig.action}]** {sig.code} {sig.name}")
            else:
                st.info("暂无交易信号")
            
            # 风险预警
            st.subheader("风险预警")
            for alert in decision.risk_alerts:
                if alert.severity == "CRITICAL":
                    st.error(f"🚨 {alert.message}")
                elif alert.severity == "HIGH":
                    st.warning(f"⚠️ {alert.message}")
            
            # 导出报告
            report_path = monitor.export_report(decision)
            st.info(f"报告已保存: {report_path}")
```

### 定时自动决策(后台运行)

```python
# 文件: auto_intraday_decision.py
import schedule
import time
from utils.intraday_decision import IntradayDecisionMonitor

def intraday_decision_job():
    """盘中定时决策"""
    print(f"\n[{datetime.now()}] 开始盘中决策检查...")
    
    try:
        monitor = IntradayDecisionMonitor(
            api_model='glm-4-plus',
            enable_notifications=True,
        )
        
        # 生成决策
        decision = monitor.generate_decision()
        
        if decision:
            # 检查风险预警
            critical_alerts = [
                a for a in decision.risk_alerts 
                if a.severity in ['CRITICAL', 'HIGH']
            ]
            
            if critical_alerts:
                print(f"\n⚠️  发现 {len(critical_alerts)} 条高风险预警:")
                for alert in critical_alerts:
                    print(f"  [{alert.severity}] {alert.message}")
                    # TODO: 发送邮件/微信通知
                    # send_notification(alert)
            
            # 导出报告
            report_path = monitor.export_report(decision)
            print(f"报告已保存: {report_path}")
    
    except Exception as e:
        print(f"决策生成失败: {e}")

# 设置定时任务
# 交易时段每小时检查一次
schedule.every().day.at("09:35").do(intraday_decision_job)  # 开盘后
schedule.every().day.at("10:35").do(intraday_decision_job)
schedule.every().day.at("11:35").do(intraday_decision_job)
schedule.every().day.at("13:05").do(intraday_decision_job)  # 午后
schedule.every().day.at("14:05").do(intraday_decision_job)
schedule.every().day.at("14:55").do(intraday_decision_job)  # 收盘前

# 启动
print("盘中定时决策引擎已启动...")
while True:
    schedule.run_pending()
    time.sleep(60)
```

---

## 📊 输出文件格式

决策报告保存到: `reports/YYYY-MM-DD/盘中决策_YYYYMMDD_HHMMSS.md`

**报告内容:**

```markdown
# AI 盘中实时决策报告

**生成时间**: 2026-06-23 19:13:06
**AI 置信度**: 75.50%
**决策次数**: #1

## 市场概况

当前持仓仓位过高，存在较大风险，建议立即减仓至目标仓位...

## 交易信号

| 代码 | 名称 | 动作 | 当前仓位 | 目标仓位 | 数量 | 置信度 | 紧急程度 |
|------|------|------|---------|---------|------|--------|----------|

## 风险预警

- 🚨 [CRITICAL] 204007亏损-92.77%，触发止损线，建议立即清仓
- ⚠️ [HIGH] 科技板块仓位已达35%，接近上限，建议控制新增买入

## 组合调整建议

- 建议将现金比例从5%提升至8%
- 减持所有亏损标的，增持现金比例

## 宏观展望

- 短期：市场震荡，关注关键支撑位
- 中期：结构性行情，关注政策导向和行业基本面
```

---

## ⚙️ 配置选项

### 1. 调整检查频率

```python
# 快速检查(每分钟)
monitor = IntradayDecisionMonitor(check_interval=60)

# 常规检查(每5分钟)
monitor = IntradayDecisionMonitor(check_interval=300)

# 保守检查(每30分钟)
monitor = IntradayDecisionMonitor(check_interval=1800)
```

### 2. 调整AI模型

```python
# 高质量模型(推荐)
monitor = IntradayDecisionMonitor(api_model='glm-4-plus')

# 快速模型(成本低)
monitor = IntradayDecisionMonitor(api_model='glm-4-flash')
```

### 3. 调整置信度阈值

```python
# 只执行高置信度决策
monitor = IntradayDecisionMonitor(min_confidence=0.8)

# 执行所有决策
monitor = IntradayDecisionMonitor(min_confidence=0.0)
```

### 4. 启用通知

```python
# 启用风险预警通知
monitor = IntradayDecisionMonitor(enable_notifications=True)

# 禁用通知(静默模式)
monitor = IntradayDecisionMonitor(enable_notifications=False)
```

---

## 🎯 使用场景

### 场景1: 盘前决策(每天9:00)

```python
# 盘前生成今日交易计划
monitor = IntradayDecisionMonitor()
monitor.load_positions()
decision = monitor.generate_decision()

# 输出今日操作计划
for sig in decision.trading_signals:
    if sig.action == "BUY":
        print(f"今日计划买入: {sig.code} {sig.name}")
```

### 场景2: 盘中监控(每小时)

```python
# 每小时检查一次风险
while trading_hours:
    decision = monitor.generate_decision()
    
    # 检查风险预警
    for alert in decision.risk_alerts:
        if alert.severity == "CRITICAL":
            execute_emergency_action(alert)  # 紧急操作
```

### 场景3: 盘后复盘(每天15:30)

```python
# 盘后生成完整分析报告
decision = monitor.generate_decision()
report_path = monitor.export_report(decision)

# 发送复盘报告
send_report_via_wechat(report_path)
```

---

## 📈 性能指标

| 指标 | 数值 |
|------|------|
| 监控器初始化时间 | <1秒 |
| 持仓数据加载 | <1秒 |
| 实时价格获取(22只) | 3-5秒 |
| AI决策生成 | 10-30秒 |
| 报告导出 | <1秒 |
| 单次决策总耗时 | 15-40秒 |
| 单次API调用成本 | ¥0.01-0.05 |
| 日均调用次数 | 6-12次 |
| 月成本估算 | ¥20-60 |

---

## ⚠️ 注意事项

### 1. API余额

当前API Key余额不足时会触发降级。解决方法:
- 登录智谱AI控制台充值
- 或切换到`glm-4-flash`模型(成本更低)

### 2. 网络依赖

- 新浪API需要网络连接
- GLM5 API需要网络连接
- 网络故障时会自动跳过决策生成

### 3. 决策可靠性

- AI决策仅供参考,**必须人工审核**
- 置信度低于0.6的建议需谨慎对待
- 高风险预警(CRITICAL/HIGH)应立即处理

### 4. 风控优先

- 永远不要完全依赖AI决策
- 设置硬性止损线,不受AI建议影响
- 重大决策(如清仓)必须人工确认

---

## 🔧 故障排查

### Q1: API Key加载失败

**错误**: `API 模式需要提供 api_key`

**解决**:
```python
# 检查.env文件是否存在
import os
print(os.path.exists('.env'))

# 检查API Key是否已设置
print(os.environ.get('ZHIPUAI_API_KEY', ''))

# 手动设置
os.environ['ZHIPUAI_API_KEY'] = 'your_api_key'
```

### Q2: 实时价格获取失败

**错误**: `未能获取到实时价格`

**解决**:
- 检查网络连接
- 确认股票代码格式正确
- 查看防火墙是否阻止新浪API访问

### Q3: 决策生成速度慢

**原因**: GLM5分析需要10-30秒

**优化**:
```python
# 使用快速模型
monitor = IntradayDecisionMonitor(api_model='glm-4-flash')

# 增加检查间隔
monitor = IntradayDecisionMonitor(check_interval=600)  # 10分钟
```

### Q4: 交易信号为空

**原因**: 当前持仓无需调整

**解决**: 这是正常现象,说明市场稳定,无需操作

---

## 📚 相关文件

| 文件 | 路径 | 说明 |
|------|------|------|
| 盘中决策模块 | `utils/intraday_decision.py` | 核心模块 |
| 快速测试脚本 | `intraday_decision_test.py` | 测试脚本 |
| GLM5客户端 | `utils/glm5_client.py` | AI客户端 |
| GLM5决策引擎 | `utils/glm5_decision_engine.py` | 决策引擎 |
| 使用指南 | `GLM5_自动决策_使用指南.md` | 详细文档 |
| 集成报告 | `GLM5_集成完成报告.md` | 配置报告 |
| 系统检查 | `GLM5_系统检查报告.md` | 检查报告 |
| 自动决策报告 | `GLM5_自动决策_完成报告.md` | 功能报告 |

---

## 🎓 下一步

1. ✅ **立即体验**: 运行 `python intraday_decision_test.py`
2. ✅ **阅读文档**: 查看 `GLM5_自动决策_使用指南.md`
3. ✅ **集成系统**: 将决策引擎添加到日报生成流程
4. ✅ **设置定时**: 配置定时任务自动运行决策
5. ✅ **调整参数**: 根据实际需求优化风控规则
6. ✅ **启用通知**: 配置邮件/微信风险预警通知

---

**配置时间**: 2026-06-23  
**测试状态**: ✅ 已通过  
**可用模型**: glm-4-plus (主), glm-4-flash (备选)

**祝使用愉快!** 🚀
